# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# Arweave wallet keyfile — never commit
wallet.json

# incremental deploy state (content hash -> arweave txid)
.deploy-cache.json
.permaweb-cache.json

# permaweb deploy secrets — never commit
.env.permaweb

# macOS-specific files
.DS_Store

.vercel

package-lock.json
bun.lockb
yarn.lock

# ide
.idea
*.iml
