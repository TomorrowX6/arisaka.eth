# 专业 / 顶级玩家扩展

基线：PR #13，`0ddfdb8`，26 关、31 个桌面应用。工作截止时间：2026-09-16 11:00 UTC+8（03:00 UTC）。

## 交付边界

- 分段建立 stacked PR，第一段以 `feat/plasma-expert-campaign` 为基线；不自动合并或更换生产种子。按用户后续要求同步部署 Cloudflare；先验证生产种子、版本兼容和会话密钥沿用，不以新种子覆盖现网存档。
- 26 关既有编号、附件与种子派生保持不变。第 26 关是原战役收束点，不挪到新关卡之后。
- 仅对显式声明兼容的、同一种子的历史 edition 迁移存档；保留玩家、尝试次数、时间、实验状态和本地文件。新关卡继续顺序鉴权。
- 难度来自真实格式、协议、密码学与跨文件推理，不靠猜谜、隐藏必要参数、答案泄漏或不可控的暴力算力。
- 每关提供独立维护端解码器、多个随机种子验证、篡改/截断回归；解码器不进入静态发布目录。
- 新应用必须有实际数据处理能力、工作区文件互通、键盘操作、失败反馈和有界资源占用；不得内置关卡答案或专用一键解题。

## 分段

1. 专业延伸：追加第一组高难度资料；兼容旧存档、修正生成流程副作用，增加通用数据分析应用。
2. 顶级延伸：追加需要组合推理的协议 / 密码学 / 逆向关卡，并补齐对应的通用工具。
3. 桌面与鲁棒性：打磨新旧应用互通、边界情况、窄屏与键盘、错误恢复；完整战役、Worker、构建、浏览器回归。

各 PR 记录实际完成范围和实际测试结果，不将计划作为已交付功能。

## 第一段：专业延伸（29 关 / 33 应用）

- 27：QUIC v1 Initial / Retry、包头保护、截断包号重建、乱序 CRYPTO 与 TLS 握手转录。
- 28：DNSSEC Ed25519 信任链、DS / DNSKEY rollover、通配符规范化、TTL 与签名时间验证。
- 29：VCD / SPI mode 3、地址模式切换、多次采样纠错、签名启动槽与防回滚策略。
- 数据工坊：有界且可取消的 Worker 配方，完整二进制输入输出、导入导出与工作区互通。
- 逻辑分析仪：精确时间轴、数字波形与全量 SPI 解码、缩放 / 游标 / 键盘及导出。
- 兼容 26 关同种子存档，保留历史通关凭证；普通生成、测试、构建不改生产入口。

本地验证（2026-09-15 UTC）：

- `pnpm check`、`pnpm test`、`pnpm build` 通过；Node 75 通过 / 1 个可选浏览器测试跳过，Worker 122 通过。
- 新应用浏览器专项 4 / 4 通过；其后的密集波形渲染与取消提示微调已随完整 CI 复验。
- `git diff --check` 通过；生产入口文件未修改。
- 本地完整桌面 / 战役运行曾因共享机器内存压力中断；未将中断计为通过。随后独立 CI 完整回归 77 / 77 通过，包含 29 关独立解码与新应用；原版音乐界面专项 17 / 17 通过。
- [PR #14](https://github.com/TomorrowX6/arisaka.eth/pull/14)，堆叠在 #13 上；[CI 34998316997](https://github.com/TomorrowX6/arisaka.eth/actions/runs/34998316997) 的 Validate 与 CTF 作业均通过。未合并、未部署。

后续 CI 复跑发现启动竞态：初始导航未结束时已显示桌面，迟到的窗口恢复会关闭玩家刚打开的启动器。修复将加载界面保留到初始导航结束，并加入延迟接口回归；不通过延长超时或强制点击隐藏问题。

## 第二段：顶级延伸（31 关 / 35 应用）

- 30：FROST(secp256k1, SHA-256)，遵循 RFC 9591 的 XMD 标量哈希、绑定因子与签名。三份当前刷新时期的 share 必须分别从三组独立 nonce-pair 方程恢复；旧时期转录、变化的签名集合、乱序 CBOR、CRC 完整但签名无效的响应均需正确处理。
- 31：GF(2^16) 中的交织 Reed–Solomon 取证，反解未知总线线性基、白化与 lane 轮转，纠正 14 个未知错误位置及 3 个擦除，并认证恢复的 DER 封装。序号经过 32 位回绕，时间戳超出 JavaScript Number 安全范围。
- 结构检查器：DER / CBOR 的通用树形语法分析，精确字节区间、64 位数值、分块、键盘、筛选、分页和完整节点报告。预览与无损字节保存明确区分；不声称验证证书信任。
- 离散数学工坊：模矩阵行变换 / 解空间、广义 CRT、精确 LLL（含幺模变换）与二进制多项式。输入不执行代码；任务可复现，运算有界且可取消。
- 兼容列表扩展至 26 / 29 关同种子 edition；历史通关记录不互相覆盖，原 29 关内容和答案摘要锁定。

当前本地验证：`pnpm check`、`pnpm test`、`pnpm build` 通过，Node 90 通过 / 1 可选浏览器跳过，Worker 123 通过。新顶级工具浏览器专项 5 / 5 通过，已检查桌面 / 窄屏截图；专业工具与启动竞态专项复验 5 / 5 通过。完整 CI 仍待验证，最终结果随 PR 更新。

第二段后续验证：[PR #15](https://github.com/TomorrowX6/arisaka.eth/pull/15) 的 [CI 35004299707](https://github.com/TomorrowX6/arisaka.eth/actions/runs/35004299707) 全绿，完整 31 关 / 桌面 / 运行时边界 85 / 85，原版音乐 17 / 17，Worker 123 / 123。

## 第三段：逆向与跨关卡取证（32 关 / 36 应用）

- 32：真实 ELF64 ET_REL / EM_BPF 对象，本地函数 / 只读数据的 LLVM REL 重定位、ALU32 与 ALU64、独立调用栈、变轮数 ARX / CBC / 字节接线逆变换。接受的 16 字节包材料必须和第 30 关群标量、第 31 关认证材料组合，才能解封归档。
- eBPF 调试器：真实有界解释执行、精确 64 位寄存器、未初始化栈检测、断点与键盘单步、符号 / 分支导航、内存及完整报告导出；普通通用工具，不包含关卡逆算法。明确不等同 Linux verifier / JIT，不执行平台 helpers / maps / CO-RE。
- 保持此前 31 关附件逐字节一致；新增 31 关完成存档的并发迁移、历史凭证和 eviction 回归。
- 部署预检拒绝不兼容线上版本；更新时沿用线上 `SESSION_SECRET`，不因缺少本地副本重新生成。应用资源发布后再次检查桌面版本，避免准备期间被其他发布覆盖。
- 本地 BPF / 跨关卡独立恢复专项 14 / 14 通过；部署健康和安全预检 7 / 7 通过。首轮浏览器专项复现了单步后键盘焦点丢失，修复后 6 / 6 通过并检查了窄屏截图；随后增加表格节点复用和双击分支回归，随最终 CI 复验。没有将失败轮次记作通过。
- Cloudflare 账户已验证可用。当前本地种子与现网 26 关 edition 不同，部署预检将拒绝覆盖；已向用户请求原生产种子 / 路径。实际部署和最终 CI 结果另行记录。
